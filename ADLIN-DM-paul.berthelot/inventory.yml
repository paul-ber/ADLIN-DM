all:
  children:
    dns_servers:
      hosts:
        dns-server:
          ansible_host: 10.42.8.2
          ansible_user: ansible
          ansible_password: ansible
          ansible_become_pass: ansible
      vars:
        student_number: 8